Open the existing mattdannadev/Salad-Soulmates repository. This is a production-quality refactor of the current codebase, not a new app or a feature expansion.

Read the attached APPLY_AND_CONTINUE.md. Preserve current work and inspect the current branch. Review and safely apply salad-soulmates-hardening.patch on a new refactor branch only if it fits the current source; reconcile newer code rather than replacing it with an older snapshot. Read the root AGENTS.md and treat it as the mandatory standard for both existing and future code without requiring me to repeat it.

Complete the remaining work in docs/engineering-refactor.md. Audit all existing application code, add characterization/regression tests, refactor or selectively rewrite deficient modules, and configure the full compatible Airbnb JavaScript/React rules plus strong TypeScript checks. Do not stop at formatting, partial lint rules, or adding instructions. Validate inputs, handle SDK errors and rejected promises, eliminate silent failures, and maintain focused, well-typed modules with meaningful constants and documentation. Inspect existing decisions before asking questions; ask only about consequential requirements that remain genuinely unresolved.

Preserve current UI workflows, business behavior, data, organization/facility isolation, the approved lot/date rules, recipe/unit handling, and incremental delivery. Do not create another hosted database, seed shared data, or deploy to production. Run the complete npm run check with the pinned dependencies, then relevant browser and disposable local-database integration tests. Fix failures without weakening checks. Report actual changes, exact verification results, remaining risks, and anything still unverified. Keep the pull request in draft until verification is complete. Do not merge or deploy automatically.
